class Solution {
    public int singleNumber(int[] nums) {
        
        int result = 0; // stores XOR result
        
        for(int num : nums) {
            result ^= num; // XOR each element
        }
        
        return result; // final answer
    }
}
